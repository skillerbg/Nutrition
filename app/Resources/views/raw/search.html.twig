[{% for author in authors -%}
    {{ {id: recipe.id, text: recipe.__toString}|json_encode|raw }}
    {# use the following for jquery-ui
    {{ {value: author.id, label: author.__toString}|json_encode|recipe }}
    #}
    {%- if not loop.last %},{% endif -%}
{%- endfor %}]
